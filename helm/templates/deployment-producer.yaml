apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "helm.fullname" . }}-producer
  labels:
{{ include "helm.labels" . | indent 4 }}
spec:
  replicas: {{ .Values.producer.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "helm.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "helm.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
    {{- with .Values.producer.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.producer.image.repository }}:{{ .Values.producer.image.tag }}"
          imagePullPolicy: {{ .Values.producer.image.pullPolicy }}
          env:
            - name: RABBITMQ_SERVICE_URL
              value: {{ .Values.producer.env.RABBITMQ_SERVICE_URL | quote }}
            - name: RABBITMQ_SERVICE_PORT
              value: {{ .Values.producer.env.RABBITMQ_SERVICE_PORT | quote }}
            - name: RABBITMQ_QUEUE
              value: {{ .Values.producer.env.RABBITMQ_QUEUE | quote }}
            - name: RABBITMQ_EXCHANGE
              value: {{ .Values.producer.env.RABBITMQ_EXCHANGE | quote }}
            - name: RABBITMQ_SECRET_PATH
              value: {{ .Values.producer.volume.mountPath | quote }}
#          ports:
#            - name: http
#              containerPort: 80
#              protocol: TCP
#          livenessProbe:
#            httpGet:
#              path: /
#              port: http
#          readinessProbe:
#            httpGet:
#              path: /
#              port: http
          volumeMounts:
            - mountPath: /dev/i2c-1
              name: dev-i2c
            - name: rabbitmq-credentials
              mountPath: {{ .Values.producer.volume.mountPath }}
          securityContext:
            privileged: true
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: dev-i2c
          hostPath:
            path: {{ .Values.producer.i2c.path }}
        - name: rabbitmq-credentials
          secret:
            secretName: {{ .Values.producer.secretName }}
            items:
              - key: rabbitmq-username
                path: username
              - key: rabbitmq-password
                path: password
      {{- with .Values.producer.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
